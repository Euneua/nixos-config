-- input/mouse.lua

hl.config({
  input = {
    follow_mouse   = 1,     -- Focus follows mouse
    mouse_refocus  = true,  -- Re-focus when mouse enters window
    focus_on_close = 1,     -- Focus window under cursor on close
    sensitivity    = 0,     -- No sensitivity modification (-1.0 to 1.0)
    accel_profile  = "flat", -- No acceleration
  },
})
