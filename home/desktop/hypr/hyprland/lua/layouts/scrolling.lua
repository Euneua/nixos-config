-- layouts/scrolling.lua

hl.config({
  scrolling = {
    fullscreen_on_one_column = true, -- Fullscreen when only one column visible
    column_default_width     = 0,    -- 0 = auto width
    right_is_master          = true, -- New windows open on the right
  },
})
